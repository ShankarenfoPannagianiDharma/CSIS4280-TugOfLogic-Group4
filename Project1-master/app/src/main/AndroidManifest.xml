<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.project1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".EditRipActivity"></activity>
        <activity android:name=".BoutAdminViewActivity" />
        <activity android:name=".BoutPlayerViewActivity" />
        <activity android:name=".FinalTallyPlayerViewActivity" />
        <activity android:name=".InitialTallyAdminViewActivity" />
        <activity android:name=".InitialTallyPlayerViewActivity" />
        <activity android:name=".StatementActivity" />
        <activity android:name=".WaitActivity" />
        <activity android:name=".ClaimActivity" />
        <activity android:name=".GameActivity" />
        <activity android:name=".Login" /> <!-- The below is for the splash screen and we need no action bar and the default theme -->
        <activity
            android:name=".SplashActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>